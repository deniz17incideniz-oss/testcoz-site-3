import { findUser, getRows } from "./_lib/sheets.js";
import { bodyTooLarge, clean, originAllowed, rateLimited, sessionCookie, signSession, validEmail, verifyPassword } from "./_lib/security.js";
function reply(res,status,message){res.status(status).json({success:status>=200&&status<300,message});}
export default async function handler(req,res){res.setHeader("Cache-Control","no-store");res.setHeader("X-Content-Type-Options","nosniff");if(req.method!=="POST")return reply(res,405,"Yalnızca POST isteği kabul edilir.");if(!originAllowed(req))return reply(res,403,"İsteğe izin verilmedi.");if(bodyTooLarge(req))return reply(res,413,"Gönderilen veri çok büyük.");if(rateLimited(req,10))return reply(res,429,"Çok fazla deneme yapıldı. Lütfen daha sonra tekrar deneyin.");const email=clean(req.body?.email,120).toLowerCase(),password=String(req.body?.password||"");if(!email||!password||!validEmail(email)||password.length>128)return reply(res,400,"E-posta veya şifre hatalı.");const secret=process.env.JWT_SECRET;if(!secret||secret.length<32)return reply(res,500,"Giriş işlemi şu anda tamamlanamıyor.");try{const {rows}=await getRows(),user=findUser(rows,email);if(!user||String(user[9]||"").toLocaleLowerCase("tr-TR")!=="aktif"||!(await verifyPassword(password,user[6])))return reply(res,401,"E-posta veya şifre hatalı.");res.setHeader("Set-Cookie",sessionCookie(signSession(email,secret)));return reply(res,200,"Giriş başarılı. Öğrenci paneline yönlendiriliyorsunuz.");}catch{return reply(res,500,"Giriş işlemi sırasında bir sorun oluştu. Lütfen daha sonra tekrar deneyin.");}}
